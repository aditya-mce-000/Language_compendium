#include <stdio.h>

int main(void)
{
    // int a = 10, b = 3;
    // printf("%d\n", a + b * 2);//10+3*2 = 16(operator precedence /%* is bigger than +- is bigger than =)
    // int a = 10, b = 3;
    // printf("%d\n", a / b);//3

    // int a = 10;
    // float b = 3;
    // printf("%.2f\n", a / b);//3.33

    // int a = 5, b = 2;
    // printf("%d\n", a > 3 && b < 5);//1

    // int a = 5, b = 10;
    // printf("%d\n", a == 5 || b == 5);//1

    // int x = 5;
    // printf("%d\n", x++ + ++x);//12// pre and post// not realiable to use it like this
//     Prefix (++x): Increments the variable value before evaluating its value in the expression.

// Postfix (x++): Evaluates the current variable value first, then increments it afterwards.
    return 0;
}