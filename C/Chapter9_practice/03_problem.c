#include <stdio.h>

int main(void)
{
    // Array
    //Why Choose an Array?
    // Same Data Type: An array holds multiple values of the exact same type, like integers.
    // Easy Access: You can access any integer quickly using an index number (like arr[0] or arr[19]).
    // Loops: You can use a simple loop to read or print all twenty numbers very fast.
    // Memory Efficiency: Array items sit right next to each other in memory, which helps the computer run your program smoothly.
    return 0;
}